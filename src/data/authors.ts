// Minimal stub - actual author data is fetched from D1 database via useAuthors hook
import { AuthorInfo } from "@/types/blog";

// Empty fallback - actual data comes from D1 database
export const authors: Record<string, AuthorInfo> = {};
